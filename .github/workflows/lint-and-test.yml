name: Lint & Test

on:
  workflow_call:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-node-env

      - run: npm run lint
      - run: npm run test
